export enum EnumScreen {
  MAIN = 'Main',
  HOME = 'Home',
  LOANS = 'Loans',
  BLOG = 'Blog',
  FAQ = 'FAQ',
}

export enum EnumStatusInput {
  SUCCESS = 'success',
  ERROR = 'error',
}

export enum EnumStatus {
  LOADING = 'loading',
  ERROR = 'error',
  SUCCESS = 'success',
  DEFAULT = 'default',
}
